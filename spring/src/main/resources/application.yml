spring:
  datasource:
    url: jdbc:mysql://hojun300-database.cd8qcimkew4w.ap-northeast-2.rds.amazonaws.com/HACKATHON
    username: root
    password: dlghwns1!
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: never
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
        show_sql: true
        format_sql: true
        use_sql_comments: true
        hbm2ddl:
          auto: create
        default_batch_fetch_size: 1000